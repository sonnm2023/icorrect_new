import 'package:flutter/material.dart';

class UserAuthenStatusUI {
  String title;
  String description;
  IconData icon;
  Color backgroundColor;
  Color titleColor;
  Color iconColor;

  UserAuthenStatusUI(
      {required this.title,
      required this.description,
      required this.icon,
      required this.backgroundColor,
      required this.titleColor,
      required this.iconColor});
  
}
